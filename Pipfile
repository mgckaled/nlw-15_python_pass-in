[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
dev = "python case.py"
test = "pytest"
test-list = "pytest -s -v"

[packages]
sqlalchemy = "*"
pytest = "*"
pre-commit = "*"
pylint = "*"

[dev-packages]

[requires]
python_version = "3.11"
python_full_version = "3.11.5"
